spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/book_social_network
    username: username
    password: password
    driver-class-name: org.postgresql.Driver
  application:
    name: book-network-api
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
brevo:
  api-key: ${BREVO_API_KEY}
  sender-email: ${BREVO_SENDER_EMAIL}
  sender-name: ${BREVO_SENDER_NAME}

# Cloudinary configuration (for development)
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME}
  api-key: ${CLOUDINARY_API_KEY}
  api-secret: ${CLOUDINARY_API_SECRET}

application:
  security:
    jwt:
      secret-key: a058d120bfbcd11b1633c7616d55b77b1917a8edd8fd384a574d3037be429e42
      expiration: 8640000
  mailing:
    frontend:
      activation-url: http://localhost:4200/activate-account?context=activation
      reset-url: http://localhost:4200/activate-account?context=reset
      add-url: http://localhost:4200/add-password
  file:
    storage:
      type: cloudinary
server:
  port: 8088